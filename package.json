{
  "name": "ferrisljh93-profile",
  "version": "1.0.0",
  "description": "Development tooling for the ferrisljh93 GitHub profile README: lint and GitHub-styled live preview.",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "markdownlint \"**/*.md\" --ignore node_modules",
    "lint:fix": "markdownlint \"**/*.md\" --ignore node_modules --fix",
    "preview": "node scripts/preview.mjs"
  },
  "devDependencies": {
    "github-markdown-css": "^5.8.1",
    "markdownlint-cli": "^0.42.0",
    "marked": "^14.1.3",
    "marked-gfm-heading-id": "^4.1.1"
  }
}
